{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": [],
      "env": ["NODE_ENV", "NEXT_PUBLIC_*", "AWS_*", "BUCKET"],
      "outputs": []
    },
    "lint": {},
    "test": {
      "dependsOn": ["build"]
    }
  }
} 